@using MvcKutuphane.Models.Entity
@model List<PERSONEL>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layaout.cshtml";
}

<style>
    #strip {
        width: 100%;
        height: 40px;
        padding: 8px;
        background: #d4d4d4;
        color: black;
        text-align: center;
        font-size: 18px
    }
</style>
<br />
<div id="strip">Personel Listesi</div>
<br />
<table class="table table-active">
    <tr>
        <th>ID</th>
        <th>Personel Adı Soyadı</th>
        <th>Sil</th>
        <th>Güncelle</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ID</td>
            <td>@item.PERSONEL1</td>
            <td>@Html.ActionLink("Sil", "DeleteEmployee", new { id = item.ID }, new { @class = "btn btn-danger", onclick = "return confirm('Silmek İstiyor musunuz ?')" })</td>
            <td><a href="/Employee/UpdateEmployee/@item.ID" class="btn btn-warning">Güncelle</a></td>
        </tr>

    }
</table>
<a href="/Employee/AddEmployee/" class="btn btn-success" style="width:100%">Yeni Personel Ekle</a>



